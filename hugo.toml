baseURL = "https://frothlang.org/"
languageCode = "en-us"
title = "Froth"
relativeURLs = true

[outputs]
  home = ["HTML", "RSS", "JSON"]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
    style = "monokailight"
    lineNos = false
    noClasses = false

[params]
  description = "A programming language that turns your microcontroller into a conversation."
